<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds only: allow HTTP for local backend testing. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
